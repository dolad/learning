# This workflow will do a clean install of node dependencies, build the source code and run tests across different versions of node
# For more information see: https://help.github.com/actions/language-and-framework-guides/using-nodejs-with-github-actions


name: Integrating

on:
  push:
    branches: [API-V1]
  pull_request:
    branches: [API-V1]

jobs:
  build:

    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [10.x, 12.x, 14.x, 15.x]
        # See supported Node.js release schedule at https://nodejs.org/en/about/releases/

    steps:
    - uses: actions/checkout@v2
    - name: Use Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v2
      with:
        node-version: ${{ matrix.node-version }}
    - uses: akhileshns/heroku-deploy@v3.12.12
      with:
          heroku_api_key: ${{secrets.HEROKU_API_KEY}}
          heroku_app_name: "lms-ukdiongroups"  #Must be unique in Heroku
          heroku_email: "it@ukdion.com"
          branch: "staging"
          justlogin: true
    - run: |
        git remote add heroku git@heroku.com:lms-ukdiongroups.git
        git config user.name davidukdion && git config user.email davidakande@ukdion.com
        git push heroku staging
    - run: npm ci
    - run: npm run lint
    - run: npm run build --if-present
